name: Inspect GitHub Pages deployment

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - name: List latest Pages runs
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh run list --repo "$GITHUB_REPOSITORY" --workflow deploy-pages.yml --limit 5 --json databaseId,status,conclusion,headSha,event,createdAt,url > pages-runs-latest.json
      - name: Upload latest Pages runs
        uses: actions/upload-artifact@v4
        with:
          name: github-pages-latest
          path: pages-runs-latest.json
